<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="styles.css">
    <title>To Do List</title>
</head>
<body>
     <div class="box" id="heading">
        <h1><%= kindsOfDay %></h1>
     </div>
    <div class="box">
           <% for (var i=0; i< newListItem.length; i++){ %>
            <div class="item">
                <input type="checkbox" >
                <p><%= newListItem[i] %></p> 
            </div>
           <% } %>

           <form action="/" method="post" class="item">
               <input type="text" name="newItem" placeholder="New Item Enter Here" autocomplete="off">
               <button type="submit" name="list" value=<%= kindsOfDay %>><b>+</b></button>
           </form>
    </div>
    
    
</body>
</html> 


<!-- <%  if(kindsOfDay==="sarurday" || kindsOfDay==="sunday"){ %>
    <h1 style="color: crimson;"> <%= kindsOfDay %> ToDo List</h1>
   <% } else{ %>
        <h1 style="color: aqua;"> <%= kindsOfDay %> ToDo List</h1>
    <% } %> -->